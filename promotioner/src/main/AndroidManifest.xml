<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.promotioner.main" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".Application.PromotionerApplication"
        android:theme="@style/AppTheme" >
        <activity
            android:theme="@style/MyTheme"
            android:name=".Activity.PromotionerAdd"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activity.PromotionerMessage"
            android:theme="@style/MyTheme"
            android:label="@string/back"
            />

        <activity android:name=".Activity.PromotionMain"
            android:theme="@style/MyTheme"
            />
    </application>

</manifest>
